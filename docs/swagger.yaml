info:
  contact: {}
paths:
  /api/login:
    get:
      consumes:
      - application/json
      description: 登录
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            type: string
      summary: ping example
      tags:
      - example
swagger: "2.0"
